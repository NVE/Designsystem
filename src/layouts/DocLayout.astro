---
const {component} = Astro.props;
let dynamicComponent;
/*
JSON Could include 

description for description layout

jsdocs but how? ingen poeng å ha 



shoelace har egen markdown for all content som bruker kunne bli asnvarlig for (kode eksemplarer)

hvordan implementerer de events properties osv



shoelace
1. lage custom elements json med web-component-analyzer
2. henter alle components i custom elements json
3. så henter man en spesifikk komponent basert på url 
4. njk slår sammen både markdown og alle andre props fra custom elements json

*/




---

